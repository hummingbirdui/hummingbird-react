---
title: Carousel
description: A swipeable slideshow for cycling through images or content, with controls, indicators, and captions, built on Embla Carousel.
---

## Default

A classic image carousel with overlay controls, bar indicators, and captions. Slides respond to drag, swipe, and arrow keys.

<ComponentPreview name="carousel-default" />

## Multiple slides

Slide widths come from basis utilities on `Carousel.Item`; `opts` forwards Embla options like `align` and `loop`.

<ComponentPreview name="carousel-gallery" />

## Autoplay

The autoplay plugin can be passed through the `plugins` prop.

<ComponentPreview name="carousel-autoplay" />

## Drag free

`dragFree: true` scrolls with free momentum instead of snapping to slides.

<ComponentPreview name="carousel-drag-free" />

## Vertical

`orientation="vertical"` scrolls along the y-axis; the container height is set on `Carousel.Content`.

<ComponentPreview name="carousel-vertical" />

## Slide counter

`setApi` exposes the Embla API for imperative control and state, such as tracking the selected slide.

<ComponentPreview name="carousel-counter" />

## Thumbnails

The Embla API also drives external navigation - here a synced thumbnail strip highlights the selected slide and jumps on click.

<ComponentPreview name="carousel-thumbnails" />

## API Reference

Built on [Embla Carousel](https://www.embla-carousel.com). The root owns the Embla instance and provides it to the parts; every Embla option and plugin passes through.

### Carousel

<PropsTable
  data={[
    {
      prop: "opts",
      type: "EmblaOptionsType",
      typeSimple: "object",
      description: (
        <>
          Options forwarded to Embla Carousel — <code>loop</code>,{" "}
          <code>align</code>, <code>dragFree</code>, <code>skipSnaps</code>, and
          more; see the options table below.
        </>
      ),
    },
    {
      prop: "plugins",
      type: "EmblaPluginType[]",
      typeSimple: "array",
      description:
        "Embla plugins, such as autoplay, class names, or wheel gestures.",
    },
    {
      prop: "orientation",
      type: '"horizontal" | "vertical"',
      typeSimple: "enum",
      default: '"horizontal"',
      description: "The scroll axis of the carousel.",
    },
    {
      prop: "setApi",
      type: "(api: CarouselApi) => void",
      typeSimple: "function",
      description:
        "Receives the Embla API instance for imperative control and state subscriptions.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

<br />

<DataAttributesTable
  data={[
    {
      attribute: "[data-orientation]",
      values: ["horizontal", "vertical"],
    },
  ]}
/>

#### Options

Keys accepted by the `opts` object, matching the [Embla v8 options](https://www.embla-carousel.com/docs/v8/api/options) one to one. `axis` is the exception — it is managed by the `orientation` prop and overwritten if set here.

<PropsTable
  data={[
    {
      prop: "active",
      type: "boolean",
      default: "true",
      description:
        "Turns the carousel on or off; when false, slides render statically and dragging is disabled.",
    },
    {
      prop: "align",
      type: '"start" | "center" | "end" | (viewSize: number, snapSize: number, index: number) => number',
      typeSimple: "enum | function",
      default: '"center"',
      description:
        "Aligns the slides relative to the viewport, or a function returning a custom offset in pixels.",
    },
    {
      prop: "breakpoints",
      type: "{ [mediaQuery: string]: EmblaOptionsType }",
      typeSimple: "object",
      default: "{}",
      description: (
        <>
          Option overrides applied at media queries, e.g.{" "}
          <code>{'{ "(min-width: 768px)": { loop: true } }'}</code>.
        </>
      ),
    },
    {
      prop: "containScroll",
      type: 'false | "trimSnaps" | "keepSnaps"',
      typeSimple: "enum",
      default: '"trimSnaps"',
      description:
        "Clears leading and trailing empty space that causes excessive scrolling; trimSnaps removes unreachable snap points, keepSnaps keeps them.",
    },
    {
      prop: "container",
      type: "string | HTMLElement | null",
      typeSimple: "string | element",
      default: "null",
      description:
        "A custom container node or selector holding the slides; defaults to the first direct child of the viewport.",
    },
    {
      prop: "direction",
      type: '"ltr" | "rtl"',
      typeSimple: "enum",
      default: '"ltr"',
      description: "The content direction of the carousel.",
    },
    {
      prop: "dragFree",
      type: "boolean",
      default: "false",
      description:
        "Scrolls with free momentum on drag instead of snapping to slides.",
    },
    {
      prop: "dragThreshold",
      type: "number",
      default: "10",
      description:
        "Drag distance in pixels before dragging starts; affects when clicks are treated as drags, not drag sensitivity.",
    },
    {
      prop: "duration",
      type: "number",
      default: "25",
      description:
        "Scroll animation speed for programmatic scrolls — an attraction factor, not milliseconds; higher is slower.",
    },
    {
      prop: "inViewThreshold",
      type: "number | number[]",
      typeSimple: "number",
      default: "0",
      description:
        "IntersectionObserver threshold deciding how much of a slide must be visible to count as in view.",
    },
    {
      prop: "loop",
      type: "boolean",
      default: "false",
      description: "Enables infinite looping through the slides.",
    },
    {
      prop: "skipSnaps",
      type: "boolean",
      default: "false",
      description:
        "Allows skipping intermediate snap points when dragging vigorously.",
    },
    {
      prop: "slides",
      type: "string | HTMLElement[] | NodeListOf<HTMLElement> | null",
      typeSimple: "string | elements",
      default: "null",
      description:
        "Custom slide nodes or a selector; defaults to the direct children of the container.",
    },
    {
      prop: "slidesToScroll",
      type: 'number | "auto"',
      typeSimple: "number | enum",
      default: "1",
      description:
        "Number of slides scrolled per step; auto groups slides by how many fit in the viewport.",
    },
    {
      prop: "startIndex",
      type: "number",
      default: "0",
      description: "Index of the slide selected on init.",
    },
    {
      prop: "watchDrag",
      type: "boolean | (emblaApi, event) => boolean | void",
      typeSimple: "boolean | function",
      default: "true",
      description:
        "Enables mouse and touch dragging, or a callback to accept or reject each drag interaction.",
    },
    {
      prop: "watchResize",
      type: "boolean | (emblaApi, entries) => boolean | void",
      typeSimple: "boolean | function",
      default: "true",
      description:
        "Reinitializes the carousel when the container or slides resize, or a callback to customize the behavior.",
    },
    {
      prop: "watchSlides",
      type: "boolean | (emblaApi, mutations) => boolean | void",
      typeSimple: "boolean | function",
      default: "true",
      description:
        "Reinitializes the carousel when slides are added or removed, or a callback to customize the behavior.",
    },
    {
      prop: "watchFocus",
      type: "boolean | (emblaApi, event) => boolean | void",
      typeSimple: "boolean | function",
      default: "true",
      description:
        "Scrolls a slide into view when it receives focus, or a callback to customize the behavior.",
    },
  ]}
/>

## Styling

The root reuses core Hummingbird's `carousel` class and its CSS variables for the controls, indicators, and caption; the Embla-specific parts add two variables of their own.

```css
.carousel {
  --carousel-control-width: 15%;
  --carousel-control-color: var(--color-white);
  --carousel-control-opacity: 0.5;
  --carousel-control-hover-opacity: 0.9;
  --carousel-control-icon-width: 2rem;
  --carousel-control-icon-height: 2rem;
  --carousel-indicator-width: 1.875rem;
  --carousel-indicator-height: 0.1875rem;
  --carousel-indicator-spacer: --spacing(0.75);
  --carousel-indicator-active-bg: var(--color-white);
  --carousel-indicator-hit-area-height: 0.625rem;
  --carousel-indicator-opacity: 0.5;
  --carousel-indicator-active-opacity: 1;
  --carousel-caption-width: 70%;
  --carousel-caption-spacer: --spacing(5);
  --carousel-caption-padding-y: --spacing(5);
  --carousel-caption-color: var(--color-white);
}
.carousel-container {
  --carousel-item-spacing: --spacing(4);
}
.carousel-control {
  --carousel-control-disabled-opacity: 0.25;
}
```
