---
title: Avatar
description: An image element with a text fallback for representing a user or entity.
---

## Default

An avatar renders an image with a fallback for when the image is unavailable.

<ComponentPreview name="avatar-default" />

## Sizes

The `size` prop offers four sizes: `xs`, `sm`, `md` (default), and `lg`.

<ComponentPreview name="avatar-sizes" />

## Status

The `status` prop adds a presence indicator: `online`, `offline`, `away`, or `busy`.

<ComponentPreview name="avatar-status" />

## Fallback

`Avatar.Fallback` renders when the image is unavailable — typically the user's initials.

<ComponentPreview name="avatar-fallback" />

## Group

`Avatar.Group` stacks several avatars into an overlapping row.

<ComponentPreview name="avatar-group" />

## API Reference

Built on the [Radix UI Avatar](https://www.radix-ui.com/primitives/docs/components/avatar) primitive. `Avatar`, `Avatar.Image`, and `Avatar.Fallback` forward all props to their Radix counterparts; `Avatar.Group` is a Hummingbird addition.

### Avatar

Contains all the parts of an avatar.

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
    },
    {
      prop: "size",
      type: '"xs" | "sm" | "md" | "lg"',
      typeSimple: "enum",
      default: '"md"',
      description: "Size of the avatar.",
    },
    {
      prop: "status",
      type: '"online" | "offline" | "away" | "busy"',
      typeSimple: "enum",
      description: "Presence indicator shown in the corner of the avatar.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

### Avatar.Image

The image to render. By default it will only render when it has loaded.

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
    },
    {
      prop: "onLoadingStatusChange",
      type: '(status: "idle" | "loading" | "loaded" | "error") => void',
      typeSimple: "function",
      description:
        "A callback providing information about the loading status of the image. This is useful in case you want to control more precisely what to render as the image is loading.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

### Avatar.Fallback

An element that renders when the image hasn't loaded. This means whilst it's loading, or if there was an error.

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
    },
    {
      prop: "delayMs",
      type: "number",
      description:
        "Useful for delaying rendering so it only appears for those with slower connections.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

### Avatar.Group

Stacks several avatars into an overlapping row. A Hummingbird addition with no Radix counterpart.

## Styling

Hummingbird React avatar is styled entirely through Hummingbird's utility classes and CSS
variables.

- See the full list of available avatar classes in the [Class overview](https://hbui.dev/docs/components/avatar/#class-overview).
- Visit the [CSS Variables](https://hbui.dev/docs/components/avatar/#css-variables) documentation to explore all available variables.
