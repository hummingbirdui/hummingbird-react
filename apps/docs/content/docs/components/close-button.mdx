---
title: Close Button
description: An icon-only button for dismissing content.
---

## Default

<ComponentPreview name="close-button-default" />

## Variants

The `variant` prop switches to `white`, keeping the icon visible on dark surfaces.

<ComponentPreview name="close-button-variants" />

## Shapes

The `shape` prop offers `default` (square) and `circle`.

<ComponentPreview name="close-button-shapes" />

## Disabled

The `disabled` prop dims the button and prevents interaction.

<ComponentPreview name="close-button-disabled" />

## API Reference

### CloseButton

<PropsTable
  data={[
    {
      prop: "variant",
      type: '"default" | "white"',
      default: '"default"',
      description: "Visual style of the close button.",
    },
    {
      prop: "shape",
      type: '"default" | "circle"',
      default: '"default"',
      description: "Shape of the close button.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

## Styling

Hummingbird React close button is styled entirely through Hummingbird's utility classes and CSS
variables.

- See the full list of available close button classes in the [Class overview](https://hbui.dev/docs/components/close-button/#class-overview).
- Visit the [CSS Variables](https://hbui.dev/docs/components/close-button/#css-variables) documentation to explore all available variables.
