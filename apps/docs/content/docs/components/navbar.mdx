---
title: Navbar
description: A responsive navigation header with a brand, links, text, and a collapsible menu.
---

## Default

The default navbar composes a `NavbarBrand` and a `NavbarNav` of `NavLink`s.

<ComponentPreview name="navbar-default" />

## Responsive collapse

`NavbarToggle` and `NavbarCollapse` hide the menu behind a toggle below the `expand` breakpoint.

<ComponentPreview name="navbar-collapse" />

## Expand

The `expand` prop controls when the navbar expands horizontally. Supported values are `sm`, `md`, `lg`, `xl`, `2xl`, and `always`.

<ComponentPreview name="navbar-expand" />

## Always collapsed

Omitting `expand` keeps the menu behind the toggle at every width, and `duration` on `NavbarCollapse` tunes the open and close speed in milliseconds.

<ComponentPreview name="navbar-always-collapsed" />

## Scrollable

The `scrollable` prop on `NavbarNav` constrains the list height and scrolls its overflow.

<ComponentPreview name="navbar-scrollable" />

## With text

`NavbarText` renders non-link content such as a signed-in label.

<ComponentPreview name="navbar-text" />

## Custom toggle

Passing children to `NavbarToggle` overrides the default hamburger glyph.

<ComponentPreview name="navbar-custom-toggle" />

## API Reference

### Navbar

The navbar is built on the [Collapsible](/docs/components/collapsible#api-reference) primitive; `open`, `defaultOpen`, and `onOpenChange` are forwarded to its root. The primitive's `disabled` prop is not forwarded.

<PropsTable
  data={[
    {
      prop: "defaultOpen",
      type: "boolean",
      description:
        "The open state of the collapsible when it is initially rendered. Use when you do not need to control its open state.",
    },
    {
      prop: "open",
      type: "boolean",
      description: (
        <>
          The controlled open state of the collapsible. Must be used in
          conjunction with <code>onOpenChange</code>.
        </>
      ),
    },
    {
      prop: "onOpenChange",
      type: "(open: boolean) => void",
      typeSimple: "function",
      description:
        "Event handler called when the open state of the collapsible changes.",
    },
    {
      prop: "expand",
      type: '"sm" | "md" | "lg" | "xl" | "2xl" | "always"',
      typeSimple: "enum",
      description: "Breakpoint at which the navbar lays out horizontally.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

### NavbarNav

<PropsTable
  data={[
    {
      prop: "scrollable",
      type: "boolean",
      default: "false",
      description: "Constrain the list height and scroll its overflow.",
    },
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description: "Render as a child element instead of the default.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

### NavbarCollapse

`NavbarCollapse` wraps the Radix [Collapsible](/docs/components/collapsible#api-reference) content.

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
    },
    {
      prop: "forceMount",
      type: "boolean",
      default: "true",
      description: (
        <>
          Used to force mounting when more control is needed. Useful when
          controlling animation with React animation libraries.{" "}
          <code>NavbarCollapse</code> sets this to <code>true</code> so the menu
          stays mounted for its CSS collapse animation.
        </>
      ),
    },
    {
      prop: "duration",
      type: "number",
      description: "Open and close animation duration in milliseconds.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>
<br />
<DataAttributesTable
  data={[
    {
      attribute: "[data-state]",
      values: ['"open"', '"closed"'],
    },
  ]}
/>
<br />
<CssVariablesTable
  data={[
    {
      cssVariable: "--radix-collapsible-content-width",
      description: (
        <>
          The width of the content when it opens/closes. Named{" "}
          <code>collapsible</code> because it comes from the underlying
          Collapsible primitive.
        </>
      ),
    },
    {
      cssVariable: "--radix-collapsible-content-height",
      description: (
        <>
          The height of the content when it opens/closes. Named{" "}
          <code>collapsible</code> because it comes from the underlying
          Collapsible primitive.
        </>
      ),
    },
  ]}
/>

### NavbarToggle

`NavbarToggle` wraps the Radix [Collapsible](/docs/components/collapsible#api-reference) trigger. It renders the default hamburger `NavbarTogglerIcon` unless children are passed, and sets `aria-label="Toggle navigation"` by default.

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>
<br />
<DataAttributesTable
  data={[
    {
      attribute: "[data-state]",
      values: ['"open"', '"closed"'],
    },
  ]}
/>

### NavbarBrand

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description: "Render as a child element instead of the default.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

`NavbarText` accepts `className`. `NavbarTogglerIcon` renders the default hamburger glyph and accepts standard `svg` props.

## Styling

Hummingbird React navbar is styled entirely through Hummingbird's utility classes and CSS
variables.

- See the full list of available navbar classes in the [Class overview](https://hbui.dev/docs/components/navbar/#class-overview).
- Visit the [CSS Variables](https://hbui.dev/docs/components/navbar/#css-variables) documentation to explore all available variables.
