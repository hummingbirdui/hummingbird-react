---
title: Breadcrumb
description: Displays the path to the current resource using a hierarchy of links.
---

## Default

The final item marks the current page with `active` and a `BreadcrumbPage`.

<ComponentPreview name="breadcrumb-default" />

## Separators

The `separator` prop on `BreadcrumbList` changes the divider between items: `slash` (default), `dashed`, or `arrow`.

<ComponentPreview name="breadcrumb-separators" />

## API Reference

### BreadcrumbList

<PropsTable
  data={[
    {
      prop: "separator",
      type: '"slash" | "dashed" | "arrow"',
      default: '"slash"',
      description: "Divider rendered between items.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

### BreadcrumbItem

<PropsTable
  data={[
    {
      prop: "active",
      type: "boolean",
      default: "false",
      description: "Marks the item as the current page.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

### BreadcrumbLink

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description: "Render as a child element instead of the default.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

<br />

`Breadcrumb` and `BreadcrumbPage` render a `<nav>` and a `<span>`, and accept `className`.

## Styling

Hummingbird React breadcrumb is styled entirely through Hummingbird's utility classes and CSS
variables.

- See the full list of available breadcrumb classes in the [Class overview](https://hbui.dev/docs/components/breadcrumb/#class-overview).
- Visit the [CSS Variables](https://hbui.dev/docs/components/breadcrumb/#css-variables) documentation to explore all available variables.
