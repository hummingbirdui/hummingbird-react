---
title: Dialog
description: A modal dialog built on Radix UI that traps focus, locks scroll, and dismisses on backdrop click or Escape.
---

## Default

<ComponentPreview name="dialog-default" className="text-center" />

## Centered

Setting `centered` on `Dialog.Content` vertically centers the dialog in the viewport.

<ComponentPreview name="dialog-centered" className="text-center" />

## Sizes

The `size` prop sets the dialog width across `"sm"`, `"md"`, `"lg"`, and `"xl"`.

<ComponentPreview name="dialog-sizes" />

## Scrollable

Setting `scrollable` keeps the header and footer fixed while a long body scrolls inside the dialog.

<ComponentPreview name="dialog-scrollable" className="text-center" />

## Fullscreen

Setting `fullscreen` expands the dialog to fill the entire viewport.

<ComponentPreview name="dialog-fullscreen" className="text-center" />

## Controlled

Passing `open` and `onOpenChange` to the `Dialog` root drives its open state externally.

<ComponentPreview name="dialog-controlled" className="text-center" />

## API Reference

Built on the [Radix UI Dialog](https://www.radix-ui.com/primitives/docs/components/dialog) primitive. Each part forwards all props to its Radix counterpart, except `Dialog.Header`, `Dialog.Body`, and `Dialog.Footer`, which are Hummingbird-specific layout parts rendered as plain `div` elements.

### Dialog

Contains all the parts of a dialog.

<PropsTable
  data={[
    {
      prop: "defaultOpen",
      type: "boolean",
      description:
        "The open state of the dialog when it is initially rendered. Use when you do not need to control its open state.",
    },
    {
      prop: "open",
      type: "boolean",
      description: (
        <>
          The controlled open state of the dialog. Must be used in conjunction
          with <code>onOpenChange</code>.
        </>
      ),
    },
    {
      prop: "onOpenChange",
      type: "(open: boolean) => void",
      typeSimple: "function",
      description:
        "Event handler called when the open state of the dialog changes.",
    },
    {
      prop: "modal",
      type: "boolean",
      default: "true",
      description:
        "The modality of the dialog. When set to true, interaction with outside elements will be disabled and only dialog content will be visible to screen readers.",
    },
  ]}
/>

### Dialog.Trigger

The button that opens the dialog.

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>
<br />
<DataAttributesTable
  data={[
    {
      attribute: "[data-state]",
      values: ['"open"', '"closed"'],
    },
  ]}
/>

### Dialog.Portal

When used, portals your overlay and content parts into the `body`. `Dialog.Content` renders this part automatically, so you rarely need to use it directly.

<PropsTable
  data={[
    {
      prop: "forceMount",
      type: "boolean",
      description: (
        <>
          Used to force mounting when more control is needed. Useful when
          controlling animation with React animation libraries. If used on this
          part, it will be inherited by <code>Dialog.Overlay</code> and{" "}
          <code>Dialog.Content</code>.
        </>
      ),
    },
    {
      prop: "container",
      type: "HTMLElement",
      default: "document.body",
      description: "Specify a container element to portal the content into.",
    },
  ]}
/>

### Dialog.Overlay

A layer that covers the inert portion of the view when the dialog is open. `Dialog.Content` renders this part automatically; use its `overlayProps` prop to customize it.

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
    },
    {
      prop: "forceMount",
      type: "boolean",
      description: (
        <>
          Used to force mounting when more control is needed. Useful when
          controlling animation with React animation libraries. It inherits from{" "}
          <code>Dialog.Portal</code>.
        </>
      ),
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>
<br />
<DataAttributesTable
  data={[
    {
      attribute: "[data-state]",
      values: ['"open"', '"closed"'],
    },
  ]}
/>

### Dialog.Content

Contains content to be rendered in the open dialog. Always renders its own `Dialog.Portal` and `Dialog.Overlay`, and wraps the content in the `modal` and `modal-dialog` elements.

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
    },
    {
      prop: "forceMount",
      type: "boolean",
      description: (
        <>
          Used to force mounting when more control is needed. Useful when
          controlling animation with React animation libraries. It inherits from{" "}
          <code>Dialog.Portal</code>.
        </>
      ),
    },
    {
      prop: "onOpenAutoFocus",
      type: "(event: Event) => void",
      typeSimple: "function",
      description: (
        <>
          Event handler called when focus moves into the component after
          opening. It can be prevented by calling{" "}
          <code>event.preventDefault</code>.
        </>
      ),
    },
    {
      prop: "onCloseAutoFocus",
      type: "(event: Event) => void",
      typeSimple: "function",
      description: (
        <>
          Event handler called when focus moves to the trigger after closing. It
          can be prevented by calling <code>event.preventDefault</code>.
        </>
      ),
    },
    {
      prop: "onEscapeKeyDown",
      type: "(event: KeyboardEvent) => void",
      typeSimple: "function",
      description: (
        <>
          Event handler called when the escape key is down. It can be prevented
          by calling <code>event.preventDefault</code>.
        </>
      ),
    },
    {
      prop: "onPointerDownOutside",
      type: "(event: PointerDownOutsideEvent) => void",
      typeSimple: "function",
      description: (
        <>
          Event handler called when a pointer event occurs outside the bounds of
          the component. It can be prevented by calling{" "}
          <code>event.preventDefault</code>.
        </>
      ),
    },
    {
      prop: "onInteractOutside",
      type: "(event: React.FocusEvent | MouseEvent | TouchEvent) => void",
      typeSimple: "function",
      description: (
        <>
          Event handler called when an interaction (pointer or focus event)
          happens outside the bounds of the component. It can be prevented by
          calling <code>event.preventDefault</code>.
        </>
      ),
    },
    {
      prop: "size",
      type: '"sm" | "md" | "lg" | "xl"',
      typeSimple: "enum",
      default: '"md"',
      description: "The width of the dialog.",
    },
    {
      prop: "centered",
      type: "boolean",
      default: "false",
      description: "Vertically centers the dialog in the viewport.",
    },
    {
      prop: "scrollable",
      type: "boolean",
      default: "false",
      description: (
        <>
          Scrolls the <code>Dialog.Body</code> while the header and footer stay
          fixed.
        </>
      ),
    },
    {
      prop: "fullscreen",
      type: 'boolean | "sm-down" | "md-down" | "lg-down" | "xl-down" | "2xl-down"',
      typeSimple: "enum",
      default: "false",
      description:
        "Expands the dialog to fill the viewport, optionally only below a breakpoint.",
    },
    {
      prop: "dialogClassName",
      type: "string",
      description: (
        <>
          Additional classes merged onto the <code>modal-dialog</code> wrapper
          element.
        </>
      ),
    },
    {
      prop: "container",
      type: "HTMLElement",
      default: "document.body",
      description: (
        <>
          Container element forwarded to the internally rendered{" "}
          <code>Dialog.Portal</code>.
        </>
      ),
    },
    {
      prop: "portalProps",
      type: 'Omit<Dialog.PortalProps, "container" | "children">',
      typeSimple: "object",
      description: (
        <>
          Extra props forwarded to the internally rendered{" "}
          <code>Dialog.Portal</code>.
        </>
      ),
    },
    {
      prop: "overlayProps",
      type: "Dialog.OverlayProps",
      typeSimple: "object",
      description: (
        <>
          Props forwarded to the internally rendered <code>Dialog.Overlay</code>
          .
        </>
      ),
    },
    {
      prop: "className",
      type: "string",
      description: (
        <>
          Additional classes merged with the generated classes. Applied to the
          inner <code>modal-content</code> element rather than the{" "}
          <code>modal-dialog</code> wrapper.
        </>
      ),
    },
  ]}
/>
<br />
<DataAttributesTable
  data={[
    {
      attribute: "[data-state]",
      values: ['"open"', '"closed"'],
    },
  ]}
/>

### Dialog.Header

Hummingbird-specific layout part. Renders a `div` with the `modal-header` class for the top area of the dialog.

### Dialog.Title

An accessible title to be announced when the dialog is opened. Renders an `h6` element by default.

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

### Dialog.Description

An optional accessible description to be announced when the dialog is opened.

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

### Dialog.Body

Hummingbird-specific layout part. Renders a `div` with the `modal-body` class for the main content of the dialog.

### Dialog.Footer

Hummingbird-specific layout part. Renders a `div` with the `modal-footer` class, typically containing action buttons.

### Dialog.Close

The button that closes the dialog.

<PropsTable
  data={[
    {
      prop: "asChild",
      type: "boolean",
      default: "false",
      description:
        "Change the default rendered element for the one passed as a child, merging their props and behavior.",
    },
    {
      prop: "className",
      type: "string",
      description: "Additional classes merged with the generated classes.",
    },
  ]}
/>

## Styling

Hummingbird React dialog is styled entirely through Hummingbird's utility classes and CSS
variables.

- See the full list of available dialog classes in the [Class overview](https://hbui.dev/docs/components/dialog/#class-overview).
- Visit the [CSS Variables](https://hbui.dev/docs/components/dialog/#css-variables) documentation to explore all available variables.
