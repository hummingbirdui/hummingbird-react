---
title: Framework Guides
description: Step-by-step guides for setting up Hummingbird React with popular React frameworks, including dark mode support.
---

import FrameworkCard from "@/components/docs/FrameworkCard";
import { frameworks } from "@/data/frameworks";

<div className="mx-auto place-items-center grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 max-w-70 sm:max-w-full gap-6">
  {frameworks.map((item) => (
    <FrameworkCard key={item.name} framework={item} />
  ))}
</div>
